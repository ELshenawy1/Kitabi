@*@using Kitabi.Settings;
@{
    ViewData["Title"] = "BookRequests";
}


@if(!Model.Any())
{
    <div class="alert  alert-warning">
        <h4 class="alert-heading">No Books!</h4>
        <p class="mb-0">No books were added yet.</p>
    </div>
}
else
{
    <div class="mt-3">
        <table class="table table-hover ">
            <tbody>
                @foreach(var book in Model)
                {
                    @*<tr class="border-1 border-success">
                    <tr class="table-secondary" id="@book.ID">
                        <td>
                            <div class="d-flex" >
                                <div class="pe-4" >
                                    <img src="@($"{FileSettings.ImagesPath}/{book.Cover}")" alt="@book.Name" style="max-width:90px"/>
                                </div>
                                <div class="d-flex flex-column justify-content-center ">
                                    <legend>@book.Name</legend>
                                    <p>@book.Category.Name</p>
                                </div>
                            </div>
                        </td>
                        <td class="align-middle">
                            <div class="d-flex justify-content-end">
                                <a asp-action="Edit" asp-route-id="@book.ID" class="btn btn-success me-2"><i class="bi bi-check-lg"></i></a>
                                <a class="btn btn-danger me-4" onclick="savelast(@book.ID)" data-bs-toggle="modal" data-bs-target="#exampleModal"><i class="bi bi-x-lg"></i></a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>


}*@